# Local Model Files

This directory intentionally does not contain model binaries.

ONNX, ORT, TFLite, PyTorch, and similar model files are ignored by Git and must
not be bundled in the public release. Use Mixelith's local model import screen
to copy a compatible user-provided ONNX file into app-private storage on
Android.
